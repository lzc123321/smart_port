<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.finalpro.mapper.securitymapper">
    <select id="SHA1" resultType="String">
        select sha1(#{code})
    </select>

    <select id="MD5" resultType="String">
        select md5(#{code})
    </select>
</mapper>